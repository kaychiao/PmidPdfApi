version: "3.5"

services:
  web:
    volumes:
      - /Users/jiaoyk/Downloads/articles:/articles
      # - /var/run/docker.sock:/var/run/docker.sock
      - /etc/passwd:/etc/passwd:ro
      - /etc/group:/etc/group:ro